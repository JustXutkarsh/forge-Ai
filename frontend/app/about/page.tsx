"use client";

import { ArrowRight, Database, GitBranch, Layers3, LockKeyhole, Search, Sparkles } from "lucide-react";

import { AppShell } from "@/components/app-shell";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";

const architecture = [
  { icon: Database, title: "Sources", body: "CSV support tickets and GitHub Issues are normalized into one ticket schema." },
  { icon: Layers3, title: "Freshness", body: "SQLite tracks record and retrieval hashes so only changed evidence is embedded." },
  { icon: Search, title: "Retrieval", body: "Local BGE embeddings search Chroma, with SQLite as the precise metadata fallback." },
  { icon: Sparkles, title: "Reasoning", body: "OpenAI plans and synthesizes grounded answers from the retrieved evidence." },
];

export default function AboutPage() {
  return <AppShell><div className="mx-auto max-w-[1100px] px-4 py-8 sm:px-6 lg:px-10 lg:py-12"><div className="mb-10 max-w-2xl"><Badge className="border-accent/20 bg-accent/10 text-accent">Forge architecture</Badge><h1 className="mt-4 text-3xl font-semibold tracking-[-.04em] text-ink sm:text-4xl">A support investigation system built to show its work.</h1><p className="mt-4 text-sm leading-7 text-muted">Forge combines exact analytics with semantic search so support questions can be answered quickly without losing the evidence trail.</p></div><div className="grid gap-4 md:grid-cols-2">{architecture.map(({ icon: Icon, title, body }, index) => <Card key={title} className="group transition hover:-translate-y-0.5 hover:border-white/20"><CardHeader><span className="flex size-9 items-center justify-center rounded-lg bg-accent/10 text-accent"><Icon size={17} /></span><span className="font-mono text-[10px] text-muted/60">0{index + 1}</span></CardHeader><CardContent><CardTitle>{title}</CardTitle><p className="mt-2 text-xs leading-6 text-muted">{body}</p></CardContent></Card>)}</div><div className="mt-5 grid gap-5 lg:grid-cols-[1fr_1fr]"><Card><CardHeader><div><CardTitle>Request flow</CardTitle><p className="mt-1 text-xs text-muted">The same engine powers CLI and API.</p></div><GitBranch size={16} className="text-accent" /></CardHeader><CardContent><div className="space-y-2">{["Question enters Forge", "Planner selects semantic or structured tools", "Chroma / SQLite return evidence", "OpenAI produces a grounded answer"].map((step, index) => <div key={step} className="flex items-center gap-3 rounded-lg border border-line bg-panel-raised/60 px-3 py-3"><span className="flex size-6 items-center justify-center rounded-md bg-white/[.06] font-mono text-[10px] text-accent">{index + 1}</span><span className="text-xs text-ink">{step}</span>{index < 3 && <ArrowRight size={13} className="ml-auto text-muted" />}</div>)}</div></CardContent></Card><Card><CardHeader><div><CardTitle>Design principles</CardTitle><p className="mt-1 text-xs text-muted">What makes Forge useful in practice.</p></div><LockKeyhole size={16} className="text-success" /></CardHeader><CardContent><ul className="space-y-3 text-xs leading-5 text-muted"><li><span className="font-medium text-ink">Grounded by default.</span> Unsupported questions return a refusal instead of fabricated confidence.</li><li><span className="font-medium text-ink">Local embeddings.</span> Semantic search has no per-vector API cost after model download.</li><li><span className="font-medium text-ink">Inspectable systems.</span> Evidence IDs, confidence, timings, and structured health are visible.</li><li><span className="font-medium text-ink">Incremental work.</span> Freshness hashes prevent unnecessary embedding jobs.</li></ul></CardContent></Card></div><div className="mt-8 flex flex-wrap gap-2"><Badge>Next.js 15</Badge><Badge>FastAPI</Badge><Badge>SQLite</Badge><Badge>ChromaDB</Badge><Badge>Sentence Transformers</Badge><Badge>OpenAI reasoning</Badge></div></div></AppShell>;
}
